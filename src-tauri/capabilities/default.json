{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions only. The app reads GPX files through its own Rust command, so no filesystem or network plugin access is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
